{
  "name": "has-changed-action",
  "version": "1.0.0",
  "description": "This action sets the `changed` output parameters if changed files match the specified pattern.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^1.1.0",
    "minimatch": "^3.0.4",
    "request": "^2.88.0",
    "request-promise": "^4.2.4"
  }
}
